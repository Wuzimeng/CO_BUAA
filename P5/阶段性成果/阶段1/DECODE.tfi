


// Instantiate the module
DECODE instance_name (
    .instr(instr), 
    .NPCOp(NPCOp), 
    .EXTOp(EXTOp), 
    .ALUOp(ALUOp), 
    .RegWrite(RegWrite), 
    .MemWrite(MemWrite), 
    .RegA3Sel(RegA3Sel), 
    .RegDataSel(RegDataSel), 
    .AluBSel(AluBSel), 
    .PcSel(PcSel)
    );


