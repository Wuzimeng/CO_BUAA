


// Instantiate the module
CPU instance_name (
    .clk(clk), 
    .reset(reset), 
    .HWInt(HWInt), 
    .PrRD(PrRD), 
    .PrWD(PrWD), 
    .PrAddr(PrAddr), 
    .PrWE(PrWE), 
    .PC_all(PC_all)
    );


